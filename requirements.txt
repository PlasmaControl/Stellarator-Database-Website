Django>=5.2
gunicorn>=21.2            # WSGI server for production

# Production (AWS) only — not needed for local dev
psycopg2-binary>=2.9      # PostgreSQL driver
django-storages>=1.14     # S3 file storage backend
boto3>=1.34               # AWS SDK (used by django-storages)
